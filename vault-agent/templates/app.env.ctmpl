# Rendered by Vault Agent. Do not edit inside the container.
NODE_ENV="production"
PRODUCTION="true"
PORT="3000"
HOSTNAME="0.0.0.0"

{{ with secret "kv/data/NEXTJS/NEXTJS-UI" -}}
{{ range $key, $value := .Data.data -}}
{{ $key }}={{ $value | toJSON }}
{{ end -}}
{{ end }}

{{ with secret "kv/data/GOOGLE_OAUTH" -}}
{{ range $key, $value := .Data.data -}}
{{ $key }}={{ $value | toJSON }}
{{ end -}}
{{ end }}

{{ with secret "kv/data/MICROSOFT_OAUTH" -}}
{{ range $key, $value := .Data.data -}}
{{ $key }}={{ $value | toJSON }}
{{ end -}}
{{ end }}
