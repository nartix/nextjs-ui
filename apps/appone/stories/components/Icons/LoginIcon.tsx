import { ActionIcon } from '@mantine/core';
import { IconLogin, IconUserFilled } from '@tabler/icons-react';

export function LoginIcon({}) {
  return (
    <ActionIcon size='38' variant='default'>
      <IconUserFilled size={22} />
    </ActionIcon>
  );
}
